package com.example.crudsqlite

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import com.example.crudsqlite.databinding.ActivityAgregarEditarBinding
import com.example.crudsqlite.databinding.ActivityMainBinding

class Agregar_Editar : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        val binding = ActivityAgregarEditarBinding.inflate(layoutInflater)
        setContentView(binding.root)






    }
}